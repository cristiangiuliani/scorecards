---
name: No commit/push without explicit request
description: Never commit or push unless the user explicitly asks to do so
type: feedback
---

Never run `git commit` or `git push` unless the user explicitly asks for it (e.g. "fai commit", "committa", "commit e push").

**Why:** User was surprised by unsolicited commits and asked to stop.

**How to apply:** After making code changes, stop and wait. Do not suggest or perform commits proactively.
