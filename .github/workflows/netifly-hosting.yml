name: Build and Deploy React App

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # manual deployment trigger

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-success: ${{ steps.build-step.outcome }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Setup Yarn
        run: |
          corepack enable
          corepack prepare yarn@4.9.4 --activate
          yarn --version

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build React app
        id: build-step
        run: yarn build

      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: react-build
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && needs.build.outputs.build-success == 'success'
    environment: Production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: react-build
          path: dist  # scarica direttamente in dist

      - name: Deploy to Netlify
        id: netlify-deploy
        uses: netlify/actions/cli@master
        with:
          args: deploy --dir=dist --prod --message '${{ github.event.head_commit.message }}'
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          COMMIT_REF: ${{ github.sha }}